---
/**
 * Purpose: Homepage — raw HTML injection from design-source/pages/Homepage.html
 * Context: Route /
 * Source: design-source/pages/Homepage.html, apps/studio/schemas/singletons/homePage.ts
 */

import { sanityClient } from 'sanity:client';
import BaseLayout from '../layouts/BaseLayout.astro';
import { HOME_PAGE_QUERY, CONDITIONS_HOME_QUERY, TESTIMONIALS_HOME_QUERY } from '../lib/queries';

interface CtaLink {
  label: string;
  href: string;
  variant?: string;
}

interface ImageWithAlt {
  asset?: { url: string };
  alt?: string;
}

interface SeoFields {
  metaTitle?: string;
  metaDescription?: string;
}

interface AudienceCard {
  tagline: string;
  heading: string;
  bodyCollapsed: string;
  bodyExpanded: string;
  image?: ImageWithAlt;
  cta?: CtaLink;
}

interface ServiceTrack {
  label: string;
  heading: string;
  body?: string;
  image?: ImageWithAlt;
  cta?: CtaLink;
}

interface ProcessStep {
  stepNumber: string;
  heading: string;
  body?: string;
}

interface HomePage {
  heroEyebrow?: string;
  heroHeadline?: string;
  heroSubhead?: string;
  heroImage?: ImageWithAlt;
  heroPrimaryCta?: CtaLink;
  heroSecondaryCta?: CtaLink;
  routerEyebrow?: string;
  routerHeading?: string;
  routerSubhead?: string;
  routerCards?: AudienceCard[];
  beliefQuote?: string;
  beliefBody?: string;
  twoWaysEyebrow?: string;
  twoWaysHeading?: string;
  twoWaysSubhead?: string;
  twoWaysTracks?: ServiceTrack[];
  conditionsEyebrow?: string;
  conditionsHeading?: string;
  conditionsSubhead?: string;
  howItWorksEyebrow?: string;
  howItWorksHeading?: string;
  teletherapyTrackLabel?: string;
  teletherapySteps?: ProcessStep[];
  teletherapyCta?: CtaLink;
  facilityTrackLabel?: string;
  facilitySteps?: ProcessStep[];
  facilityCta?: CtaLink;
  testimonialsEyebrow?: string;
  testimonialsHeading?: string;
  testimonialsSubhead?: string;
  providerTeaserEyebrow?: string;
  providerTeaserHeading?: string;
  providerTeaserBody?: string;
  providerTeaserImage?: ImageWithAlt;
  providerTeaserPrimaryCta?: CtaLink;
  providerTeaserSecondaryCta?: CtaLink;
  seo?: SeoFields;
}

interface Condition {
  _id: string;
  tagline: string;
  heading: string;
  body?: string;
  image?: ImageWithAlt;
}

interface Testimonial {
  _id: string;
  quote: string;
  authorName: string;
  authorRole?: string;
  authorOrg?: string;
  authorInitials?: string;
  audienceType?: string;
}

const [page, conditions, testimonials] = await Promise.all([
  sanityClient.fetch<HomePage>(HOME_PAGE_QUERY),
  sanityClient.fetch<Condition[]>(CONDITIONS_HOME_QUERY),
  sanityClient.fetch<Testimonial[]>(TESTIMONIALS_HOME_QUERY),
]);

// Router cards: use Sanity data when available, fall back to design-source values
const routerCards = page?.routerCards ?? [];
const twoWaysTracks = page?.twoWaysTracks ?? [];
const teletherapySteps = page?.teletherapySteps ?? [];
const facilitySteps = page?.facilitySteps ?? [];
const conditionsList = conditions ?? [];
const testimonialsList = testimonials ?? [];
---

<style is:global>
  /* ════════════════════════════════════════════════
   BETTER YOU THERAPY — Homepage
   Design system: Better You Therapy DS
   Clinical trust + human warmth
════════════════════════════════════════════════ */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    /* ── DESIGN SYSTEM TOKENS ── */
    /* Core (stable across palettes) */
    --navy: #104378;
    --navy-deep: #0a2d52;
    --navy-footer: #2d2d52;
    --white: #ffffff;
    --off-white: #f5f7fa;
    --cream: #fffcf0;
    --slate: #5a7194;
    --mist: #8fa3bf;
    --border: #e4eaf0;

    /* Warm palette (primary direction) */
    --coral: #e05555;
    --coral-hover: #c94444;
    --coral-light: #fce8e8;
    --sage: #9caf88;
    --sage-hover: #7a9468;
    --sage-light: #e8efe3;

    /* Radii — per DS: 6px default, 12px large surfaces, 999px pills */
    --r-btn: 6px;
    --r-card: 12px;
    --r-lg: 12px;
    --r-pill: 999px;

    /* Shadows — per DS */
    --shadow-sm: 0 1px 2px rgba(16, 67, 120, 0.04);
    --shadow-md: 0 4px 12px rgba(16, 67, 120, 0.08);
    --shadow-lg: 0 8px 24px rgba(16, 67, 120, 0.1);

    /* Motion — color transitions only, 150ms */
    --t-hover: 150ms ease;

    /* Layout */
    --max-w: 1200px;
    --pad-x: 64px;
    --pad-s: 80px;
  }

  html {
    scroll-behavior: smooth;
  }
  body {
    font-family:
      'Montserrat',
      system-ui,
      -apple-system,
      sans-serif;
    color: var(--slate);
    background: var(--white);
    line-height: 1.55;
    font-size: 15px;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }
  img {
    display: block;
    max-width: 100%;
  }
  a {
    text-decoration: none;
    color: inherit;
  }
  h1,
  h2,
  h3,
  h4 {
    font-family:
      'Manrope',
      system-ui,
      -apple-system,
      sans-serif;
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.01em;
    color: var(--navy-deep);
    text-wrap: balance;
  }
  p {
    text-wrap: pretty;
  }

  /* ── UTILITY — Eyebrow: Montserrat 600, UPPERCASE, 13px, .22em tracking (per UI-kit) ── */
  .eyebrow {
    font-family: 'Montserrat', sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--coral);
  }
  .max-w {
    max-width: var(--max-w);
    margin: 0 auto;
  }

  /* ── SCROLL FADE ── */
  .fade-up {
    opacity: 0;
    transform: translateY(24px);
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;
  }
  .fade-up.visible {
    opacity: 1;
    transform: none;
  }

  /* ════════════════════════════════════════════════
   BUTTONS
════════════════════════════════════════════════ */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 14px 24px;
    border-radius: var(--r-btn);
    font-family: 'Montserrat', sans-serif;
    font-size: 15px;
    font-weight: 600;
    letter-spacing: 0.01em;
    cursor: pointer;
    transition:
      background-color var(--t-hover),
      border-color var(--t-hover),
      color var(--t-hover);
    border: 1.5px solid transparent;
    white-space: nowrap;
    line-height: 1;
  }
  .btn-coral {
    background: var(--coral);
    color: #fff;
    border-color: var(--coral);
  }
  .btn-coral:hover {
    background: var(--coral-hover);
    border-color: var(--coral-hover);
  }
  .btn-ink {
    background: var(--navy);
    color: #fff;
    border-color: var(--navy);
  }
  .btn-ink:hover {
    background: var(--navy-deep);
    border-color: var(--navy-deep);
  }
  .btn-outline-white {
    background: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.5);
  }
  .btn-outline-white:hover {
    background: rgba(255, 255, 255, 0.12);
    border-color: #fff;
  }
  .btn-outline-ink {
    background: transparent;
    color: var(--navy);
    border-color: var(--navy);
  }
  .btn-outline-ink:hover {
    background: var(--navy);
    color: #fff;
  }
  .btn-outline-coral {
    background: transparent;
    color: var(--coral);
    border-color: var(--coral);
  }
  .btn-outline-coral:hover {
    background: var(--coral);
    color: #fff;
  }
  .btn-sage {
    background: var(--sage);
    color: #fff;
    border-color: var(--sage);
  }
  .btn-sage:hover {
    background: var(--sage-hover);
    border-color: var(--sage-hover);
  }
  /* legacy btn classes */
  .btn-primary {
    background: var(--coral);
    color: #fff;
    border-color: var(--coral);
  }
  .btn-primary:hover {
    background: var(--coral-hover);
    border-color: var(--coral-hover);
  }
  .btn-outline {
    background: transparent;
    color: var(--navy);
    border-color: var(--navy);
  }
  .btn-outline:hover {
    background: var(--navy);
    color: #fff;
  }
  .btn-white {
    background: #fff;
    color: var(--navy);
    border-color: #fff;
  }
  .btn-white:hover {
    background: var(--off-white);
    border-color: var(--off-white);
    color: var(--navy);
  }
  .btn-white-outline {
    background: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.55);
  }
  .btn-white-outline:hover {
    background: rgba(255, 255, 255, 0.14);
    border-color: #fff;
  }

  /* ════════════════════════════════════════════════
   NAVIGATION
════════════════════════════════════════════════ */
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--white);
    height: 84px;
    display: flex;
    align-items: center;
    padding: 0 var(--pad-x);
    border-bottom: 1px solid var(--border);
    box-shadow: var(--shadow-sm);
  }
  .nav-inner {
    max-width: var(--max-w);
    margin: 0 auto;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .nav-logo {
    display: flex;
    align-items: center;
    text-decoration: none;
    height: 68px;
  }
  .nav-logo img {
    height: 64px;
    width: auto;
    display: block;
  }
  .nav-links {
    display: flex;
    gap: 2rem;
  }
  .nav-links a {
    color: var(--navy-deep);
    font-size: 15px;
    font-weight: 500;
    transition: color var(--t-hover);
    letter-spacing: 0.01em;
  }
  .nav-links a:hover {
    color: var(--coral);
  }
  .nav-actions {
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }
  .nav-cta {
    background: var(--coral);
    color: #fff;
    padding: 12px 20px;
    border-radius: var(--r-btn);
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.01em;
    transition: background-color var(--t-hover);
  }
  .nav-cta:hover {
    background: var(--coral-hover);
  }
  .nav-cta-secondary {
    color: var(--navy-deep);
    font-size: 14px;
    font-weight: 500;
    padding: 12px 20px;
    border-radius: var(--r-btn);
    transition: color var(--t-hover);
  }
  .nav-cta-secondary:hover {
    color: var(--coral);
  }

  /* Hamburger (mobile only — hidden by default) */
  .nav-hamburger {
    display: none;
    background: transparent;
    border: none;
    cursor: pointer;
    width: 44px;
    height: 44px;
    align-items: center;
    justify-content: center;
    padding: 0;
    margin-right: -8px;
    color: var(--navy-deep);
  }
  .nav-hamburger svg {
    width: 26px;
    height: 26px;
  }

  /* Mobile menu drawer */
  .mobile-menu {
    position: fixed;
    inset: 0;
    z-index: 200;
    display: none;
    background: rgba(10, 45, 82, 0.5);
    backdrop-filter: blur(4px);
  }
  .mobile-menu.open {
    display: block;
  }
  .mobile-menu-panel {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: min(86vw, 360px);
    background: #fff;
    padding: 20px 24px 32px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    box-shadow: -8px 0 32px rgba(10, 45, 82, 0.15);
    animation: mmSlide 0.25s ease-out;
  }
  @keyframes mmSlide {
    from {
      transform: translateX(100%);
    }
    to {
      transform: none;
    }
  }
  .mobile-menu-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 16px;
    margin-bottom: 12px;
    border-bottom: 1px solid var(--border);
  }
  .mobile-menu-close {
    background: transparent;
    border: none;
    cursor: pointer;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--navy-deep);
    margin-right: -8px;
  }
  .mobile-menu-close svg {
    width: 22px;
    height: 22px;
  }
  .mobile-menu a.mm-link {
    display: block;
    padding: 14px 4px;
    font-family: 'Manrope', sans-serif;
    font-size: 18px;
    font-weight: 700;
    color: var(--navy-deep);
    border-bottom: 1px solid var(--border);
    transition: color var(--t-hover);
  }
  .mobile-menu a.mm-link:hover {
    color: var(--coral);
  }
  .mobile-menu-actions {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 20px;
  }
  .mobile-menu-actions .btn {
    width: 100%;
    justify-content: center;
  }

  /* ════════════════════════════════════════════════
   BLOCK 1 — HERO
   Left: clinician photo.  Right: belief statement on cream wash.
════════════════════════════════════════════════ */
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 88vh;
    max-height: 760px;
    background: var(--cream);
  }
  .hero-image {
    position: relative;
    overflow: hidden;
    background: var(--navy);
  }
  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }
  .hero-content {
    background: var(--cream);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 4rem 3.5rem;
    position: relative;
  }
  .hero-eyebrow {
    font-family: 'Montserrat', sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--coral);
    margin-bottom: 1.5rem;
  }
  .hero-divider {
    display: none;
  }
  .hero-content h1 {
    font-family: 'Manrope', sans-serif;
    color: var(--navy-deep);
    font-size: clamp(2.2rem, 3.6vw, 3rem);
    line-height: 1.08;
    letter-spacing: -0.02em;
    font-weight: 800;
    margin-bottom: 1.5rem;
  }
  .hero-subhead {
    color: var(--slate);
    font-size: 17px;
    line-height: 1.6;
    margin-bottom: 2.25rem;
    font-weight: 400;
    max-width: 520px;
  }
  .hero-actions {
    display: flex;
    flex-direction: row;
    gap: 0.75rem;
    align-items: center;
    flex-wrap: wrap;
  }

  /* ════════════════════════════════════════════════
   BLOCK 2 — AUDIENCE ROUTER (dominant element)
   Navy section. White cards. 1px border → 2px coral on hover + shadow-lg.
════════════════════════════════════════════════ */
  .router-section {
    background: var(--navy-deep);
    padding: var(--pad-s) var(--pad-x);
  }
  .router-header {
    text-align: center;
    margin-bottom: 48px;
  }
  .router-header .eyebrow {
    color: rgba(255, 255, 255, 0.65);
    margin-bottom: 1rem;
  }
  .router-header h2 {
    font-family: 'Manrope', sans-serif;
    font-weight: 800;
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    color: #fff;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .router-header p {
    color: rgba(255, 255, 255, 0.72);
    max-width: 520px;
    margin: 0 auto;
    font-size: 17px;
    line-height: 1.6;
  }

  .router-row {
    display: flex;
    gap: 16px;
    align-items: stretch;
    height: 380px;
    max-width: var(--max-w);
    margin: 0 auto;
  }
  .r-card {
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--r-card);
    overflow: hidden;
    background: var(--white);
    cursor: pointer;
    transition:
      flex 0.45s cubic-bezier(0.4, 0, 0.2, 1),
      border-color var(--t-hover),
      box-shadow var(--t-hover);
    position: relative;
  }
  .r-card:hover {
    border-color: rgba(224, 85, 85, 0.5);
    box-shadow: var(--shadow-lg);
  }
  .r-card.r-active {
    border-color: var(--coral);
    box-shadow: 0 8px 24px rgba(224, 85, 85, 0.18);
  }

  .r-card-wide {
    flex: 0 0 580px;
    display: flex;
    flex-direction: row;
  }
  .r-card-wide.r-inactive {
    flex: 1 0 0;
    display: block;
  }
  .r-card-narrow {
    flex: 1 0 0;
    min-width: 0;
  }
  .r-card-narrow.r-active {
    flex: 0 0 580px;
  }
  .r-wide-content {
    flex: 1 0 0;
    padding: 32px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-width: 0;
  }
  .r-wide-image {
    flex: 1 0 0;
    min-width: 0;
    background: var(--off-white) center/cover no-repeat;
  }
  .r-card-wide.r-inactive .r-wide-image {
    display: none;
  }
  .r-card-wide.r-inactive .r-wide-content {
    padding: 32px;
  }
  .r-narrow-content {
    padding: 28px;
    display: flex;
    flex-direction: column;
    gap: 14px;
    height: 100%;
    overflow: hidden;
  }

  .r-tagline {
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--coral);
    margin-bottom: 0.5rem;
  }
  .r-heading {
    font-family: 'Manrope', sans-serif;
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--navy-deep);
    line-height: 1.15;
    letter-spacing: -0.02em;
    margin-bottom: 0.6rem;
  }
  .r-body {
    font-size: 15px;
    color: var(--slate);
    line-height: 1.6;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .r-active .r-body {
    -webkit-line-clamp: unset;
    overflow: visible;
  }
  .r-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: var(--coral-light);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-bottom: 4px;
  }
  .r-icon svg {
    width: 24px;
    height: 24px;
    stroke: var(--coral);
  }
  .r-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'Montserrat', sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: #fff;
    background: var(--coral);
    letter-spacing: 0.01em;
    padding: 12px 20px;
    border-radius: var(--r-btn);
    border: none;
    cursor: pointer;
    transition: background-color var(--t-hover);
    flex-shrink: 0;
    text-decoration: none;
    margin-top: auto;
    align-self: flex-start;
  }
  .r-cta:hover {
    background: var(--coral-hover);
  }

  /* ════════════════════════════════════════════════
   BLOCK 3 — BELIEF BAND
   Coral background — full energy
════════════════════════════════════════════════ */
  .belief {
    background: var(--cream);
    padding: var(--pad-s) var(--pad-x);
    text-align: center;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .belief-inner {
    max-width: 760px;
    margin: 0 auto;
  }
  .belief-rule {
    width: 48px;
    height: 2px;
    background: var(--coral);
    margin: 0 auto 1.75rem;
    border-radius: 2px;
  }
  .belief h2 {
    color: var(--navy-deep);
    font-size: clamp(1.9rem, 3.2vw, 2.6rem);
    font-weight: 800;
    margin-bottom: 1.25rem;
    line-height: 1.15;
    letter-spacing: -0.02em;
  }
  .belief p {
    color: var(--slate);
    font-size: 17px;
    line-height: 1.7;
    font-weight: 400;
  }

  /* ════════════════════════════════════════════════
   BLOCK 4 — TWO WAYS
   Ink/dark background, full-bleed photo cards
════════════════════════════════════════════════ */
  .twoways {
    background: var(--navy-deep);
    padding: var(--pad-s) var(--pad-x) 4rem;
  }
  .twoways-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  .twoways-header .eyebrow {
    color: rgba(255, 255, 255, 0.65);
    margin-bottom: 1rem;
  }
  .twoways-header h2 {
    color: #fff;
    max-width: 900px;
    margin: 0 auto 1rem;
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    letter-spacing: -0.02em;
  }
  .twoways-header p {
    color: rgba(255, 255, 255, 0.72);
    max-width: 560px;
    margin: 0 auto;
    font-size: 17px;
    line-height: 1.6;
  }
  .twoways-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }
  .twoways-card {
    position: relative;
    border-radius: var(--r-card);
    overflow: hidden;
    height: 440px;
    cursor: pointer;
    transition:
      transform 0.3s ease,
      box-shadow var(--t-hover);
  }
  .twoways-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }
  .twoways-card-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    transition: transform 0.5s ease;
  }
  .twoways-card:hover .twoways-card-bg {
    transform: scale(1.04);
  }
  .twoways-card-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(10, 45, 82, 0.96) 30%, rgba(10, 45, 82, 0.25) 100%);
  }
  .twoways-card-content {
    position: absolute;
    inset: 0;
    padding: 2.25rem 2rem;
    color: #fff;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }
  .twoways-card-content > .twoways-card-label {
    margin-top: auto;
    margin-bottom: 0.75rem;
  }
  .twoways-card-label {
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
    margin-bottom: 0.75rem;
  }
  .twoways-card h3 {
    color: #fff;
    font-size: 1.75rem;
    margin-bottom: 0.75rem;
    line-height: 1.15;
    letter-spacing: -0.02em;
    font-weight: 800;
  }
  .twoways-card p {
    color: rgba(255, 255, 255, 0.82);
    font-size: 15px;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    max-width: 90%;
  }

  /* ════════════════════════════════════════════════
   BLOCK 5 — CONDITIONS (STICKY SCROLL)
   Cream header, alternating mint/cream sections
════════════════════════════════════════════════ */
  .l349 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
  }
  .l349-section-header {
    text-align: center;
    padding: 2.5rem var(--pad-x) 2rem;
    background: var(--white);
  }
  .l349-section-header .eyebrow {
    margin-bottom: 1rem;
  }
  .l349-section-header h2 {
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    color: var(--navy-deep);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .l349-section-header p {
    color: var(--slate);
    max-width: 560px;
    margin: 0 auto;
    font-size: 17px;
    line-height: 1.6;
  }
  .l349-left {
    display: flex;
    flex-direction: column;
  }
  .l349-section {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 80px clamp(3rem, 6vw, 6rem) 80px var(--pad-x);
  }
  .l349-section:nth-child(odd) {
    background: var(--white);
  }
  .l349-section:nth-child(even) {
    background: var(--off-white);
  }
  .l349-tagline {
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--coral);
    margin-bottom: 1rem;
  }
  .l349-h2 {
    font-family: 'Manrope', sans-serif;
    font-size: clamp(1.8rem, 3vw, 2.4rem);
    color: var(--navy-deep);
    line-height: 1.1;
    letter-spacing: -0.02em;
    font-weight: 800;
    margin-bottom: 1.25rem;
  }
  .l349-body {
    font-size: 17px;
    color: var(--slate);
    line-height: 1.65;
    margin-bottom: 2rem;
    max-width: 460px;
  }
  .l349-actions {
    display: flex;
    gap: 20px;
    align-items: center;
    flex-wrap: wrap;
  }
  .l349-btn-outline {
    font-family: 'Montserrat', sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: var(--navy-deep);
    border: 1.5px solid var(--navy-deep);
    padding: 12px 22px;
    border-radius: var(--r-btn);
    text-decoration: none;
    letter-spacing: 0.01em;
    transition:
      background-color var(--t-hover),
      color var(--t-hover);
  }
  .l349-btn-outline:hover {
    background: var(--navy-deep);
    color: #fff;
  }
  .l349-btn-ghost {
    font-family: 'Montserrat', sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: var(--coral);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: color var(--t-hover);
  }
  .l349-btn-ghost:hover {
    color: var(--coral-hover);
  }
  .l349-btn-ghost svg {
    transition: transform var(--t-hover);
  }
  .l349-btn-ghost:hover svg {
    transform: translateX(3px);
  }
  .l349-right {
    position: sticky;
    top: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0;
    background: var(--off-white);
    overflow: hidden;
  }
  .l349-img-wrap {
    position: relative;
    width: 100%;
    height: 100%;
    max-width: none;
  }
  .l349-img-wrap::before {
    display: none;
  }
  .l349-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border-radius: 0;
    opacity: 0;
    transition: opacity 0.4s ease;
    box-shadow: none;
    background-size: cover;
    background-position: center;
  }
  .l349-img.active {
    opacity: 1;
  }

  /* ════════════════════════════════════════════════
   BLOCK 6 — HOW IT WORKS
   Gold-tinted warm background, friendly track cards
════════════════════════════════════════════════ */
  .howitworks {
    background: var(--cream);
    padding: var(--pad-s) var(--pad-x);
    border-top: 1px solid var(--border);
  }
  .howitworks-header {
    text-align: center;
    margin-bottom: 3.5rem;
  }
  .howitworks-header .eyebrow {
    margin-bottom: 1rem;
  }
  .howitworks-header h2 {
    color: var(--navy-deep);
    margin-bottom: 1rem;
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    letter-spacing: -0.02em;
  }
  .howitworks-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }
  .howitworks-track {
    display: flex;
    flex-direction: column;
    background: #fff;
    border: 1px solid var(--border);
    border-radius: var(--r-card);
    padding: 2.25rem 2rem;
    box-shadow: var(--shadow-sm);
    transition:
      box-shadow var(--t-hover),
      border-color var(--t-hover);
  }
  .howitworks-track:hover {
    box-shadow: var(--shadow-md);
    border-color: var(--coral);
  }
  .howitworks-track .steps-wrap {
    flex: 1;
  }
  .howitworks-track .track-cta {
    margin-top: 1.75rem;
    text-align: center;
  }
  .tele-num {
    background: var(--sage);
    color: #fff;
  }
  .facility-num {
    background: var(--coral);
    color: #fff;
  }
  .howitworks-track-label {
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    margin-bottom: 1.75rem;
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }
  .howitworks-track-tele .howitworks-track-label {
    color: var(--sage-hover);
  }
  .howitworks-track-facility .howitworks-track-label {
    color: var(--coral);
  }
  .howitworks-track-label::after {
    content: '';
    flex: 1;
    height: 1px;
    background: currentColor;
    opacity: 0.25;
  }
  .step {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.4rem;
    align-items: flex-start;
  }
  .step:last-child {
    margin-bottom: 0;
  }
  .step-num {
    width: 32px;
    height: 32px;
    border-radius: var(--r-pill);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Manrope', sans-serif;
    font-weight: 800;
    font-size: 0.85rem;
    flex-shrink: 0;
    margin-top: 2px;
  }
  .step-text h4 {
    font-family: 'Manrope', sans-serif;
    font-size: 1rem;
    font-weight: 800;
    color: var(--navy-deep);
    margin-bottom: 0.3rem;
    letter-spacing: -0.01em;
  }
  .step-text p {
    font-size: 14px;
    color: var(--slate);
    line-height: 1.6;
  }

  /* ════════════════════════════════════════════════
   BLOCK 7 — TESTIMONIALS
   Cream bg, rounded cards, big coral quote mark
════════════════════════════════════════════════ */
  .testimonials {
    background: var(--white);
    padding: var(--pad-s) var(--pad-x);
  }
  .testimonials-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  .testimonials-header .eyebrow {
    margin-bottom: 1rem;
  }
  .testimonials-header h2 {
    color: var(--navy-deep);
    margin-bottom: 0.75rem;
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    letter-spacing: -0.02em;
  }
  .testimonials-header p {
    color: var(--slate);
    font-size: 17px;
  }
  .testimonials-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }
  .testimonial-card {
    background: var(--white);
    border-radius: var(--r-card);
    padding: 2.25rem;
    border: 1px solid var(--border);
    position: relative;
    box-shadow: var(--shadow-sm);
    transition:
      box-shadow var(--t-hover),
      border-color var(--t-hover);
  }
  .testimonial-card:hover {
    box-shadow: var(--shadow-md);
    border-color: var(--coral);
  }
  .testimonial-quote-mark {
    font-family: 'Manrope', sans-serif;
    font-size: 6rem;
    line-height: 0.5;
    color: var(--coral);
    opacity: 0.35;
    position: absolute;
    top: 1.25rem;
    left: 1.75rem;
    font-weight: 800;
  }
  .testimonial-text {
    color: var(--slate);
    font-size: 17px;
    line-height: 1.65;
    margin-bottom: 1.75rem;
    padding-top: 2rem;
    font-style: italic;
  }
  .testimonial-attribution {
    display: flex;
    align-items: center;
    gap: 0.875rem;
  }
  .testimonial-avatar {
    width: 44px;
    height: 44px;
    border-radius: var(--r-pill);
    background: var(--off-white);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Manrope', sans-serif;
    font-size: 0.9rem;
    font-weight: 800;
    color: var(--navy-deep);
    flex-shrink: 0;
  }
  .testimonial-name {
    font-family: 'Manrope', sans-serif;
    font-size: 15px;
    font-weight: 800;
    color: var(--navy-deep);
  }
  .testimonial-role {
    font-size: 13px;
    color: var(--slate);
  }
  .testimonial-placeholder {
    border: 1px dashed var(--border);
    background: var(--off-white);
    box-shadow: none;
  }
  .testimonial-placeholder:hover {
    border-color: var(--border);
    box-shadow: none;
  }
  .testimonial-placeholder .testimonial-text {
    color: var(--mist);
    font-style: normal;
    opacity: 0.75;
    font-size: 15px;
  }

  /* ════════════════════════════════════════════════
   BLOCK 8 — PROVIDER TEASER
   Sage green background — a breath of fresh air
════════════════════════════════════════════════ */
  .provider {
    background: var(--navy-deep);
    padding: var(--pad-s) var(--pad-x);
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .provider-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    opacity: 0.28;
  }
  .provider-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(10, 45, 82, 0.88) 0%, rgba(16, 67, 120, 0.82) 100%);
  }
  .provider-inner {
    max-width: 720px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }
  .provider .eyebrow {
    color: rgba(255, 255, 255, 0.72);
    margin-bottom: 1rem;
  }
  .provider h2 {
    color: #fff;
    margin-bottom: 1rem;
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    letter-spacing: -0.02em;
  }
  .provider p {
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.65;
    margin-bottom: 2rem;
    font-size: 17px;
  }
  .provider-tags {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.25rem;
  }
  .provider-tag {
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
    padding: 0.45rem 1rem;
    border-radius: var(--r-pill);
    font-size: 13px;
    font-weight: 500;
    border: 1px solid rgba(255, 255, 255, 0.18);
    letter-spacing: 0.01em;
  }
  .provider-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ════════════════════════════════════════════════
   FOOTER
   Warm ink bg — cohesive with nav
════════════════════════════════════════════════ */
  .footer {
    background: var(--navy-deep);
    color: rgba(255, 255, 255, 0.68);
    padding: 4rem var(--pad-x) 2rem;
  }
  .footer-inner {
    max-width: var(--max-w);
    margin: 0 auto;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.6fr 1fr 1fr 1.2fr;
    gap: 3rem;
    margin-bottom: 3rem;
  }
  .footer-logo {
    margin-bottom: 1rem;
    display: block;
  }
  .footer-logo img {
    height: 100px;
    width: auto;
    display: block;
  }
  .footer-tagline {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.65;
    margin-bottom: 1.5rem;
    max-width: 360px;
  }
  .footer-contact {
    font-size: 14px;
    line-height: 1.85;
    color: rgba(255, 255, 255, 0.6);
  }
  .footer-contact a {
    color: rgba(255, 255, 255, 0.6);
    transition: color var(--t-hover);
  }
  .footer-contact a:hover {
    color: #fff;
  }
  .footer-grid h4 {
    color: #fff;
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    margin-bottom: 1.25rem;
  }
  .footer-links {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }
  .footer-links a {
    color: rgba(255, 255, 255, 0.6);
    font-size: 14px;
    transition: color var(--t-hover);
  }
  .footer-links a:hover {
    color: #fff;
  }

  /* ── Footer Newsletter (column variant) ───────── */
  .footer-newsletter-pitch {
    font-size: 14px;
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.62);
    margin: -0.25rem 0 1rem;
  }
  .footer-newsletter-form {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }
  .footer-newsletter-form input {
    width: 100%;
    min-width: 0;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.16);
    color: #fff;
    font-size: 14px;
    font-family: inherit;
    padding: 11px 14px;
    border-radius: var(--r-btn);
    transition:
      border-color var(--t-hover),
      background-color var(--t-hover);
  }
  .footer-newsletter-form input::placeholder {
    color: rgba(255, 255, 255, 0.42);
  }
  .footer-newsletter-form input:focus {
    outline: none;
    border-color: var(--coral);
    background: rgba(255, 255, 255, 0.1);
  }
  .footer-newsletter-form button {
    width: 100%;
    padding: 11px 16px;
    font-size: 14px;
    font-family: 'Montserrat', sans-serif;
    font-weight: 600;
  }
  .footer-newsletter-form button:disabled {
    opacity: 0.85;
    cursor: default;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
  /* ──────────────────────────────────────────────── */

  .footer-divider {
    height: 1px;
    background: rgba(255, 255, 255, 0.1);
    margin-bottom: 1.5rem;
  }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.45);
  }
  .footer-legal {
    display: flex;
    gap: 1.5rem;
  }
  .footer-legal a {
    color: rgba(255, 255, 255, 0.45);
    transition: color var(--t-hover);
  }
  .footer-legal a:hover {
    color: #fff;
  }
  .social-links {
    display: flex;
    gap: 0.6rem;
    margin-top: 1.25rem;
  }
  .social-link {
    width: 36px;
    height: 36px;
    border-radius: var(--r-btn);
    border: 1px solid rgba(255, 255, 255, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--t-hover);
    color: rgba(255, 255, 255, 0.55);
  }
  .social-link:hover {
    border-color: rgba(255, 255, 255, 0.4);
    color: #fff;
    background: rgba(255, 255, 255, 0.06);
  }

  /* ════════════════════════════════════════════════
   RESPONSIVE
════════════════════════════════════════════════ */
  @media (max-width: 1024px) {
    :root {
      --pad-x: 32px;
      --pad-s: 64px;
    }
    /* Tablet & below: hide links + CTAs, show hamburger only */
    .nav {
      height: 72px !important;
    }
    .nav-logo {
      height: 52px !important;
    }
    .nav-logo img {
      height: 48px !important;
      width: auto !important;
      max-width: none !important;
    }
    .nav-links {
      display: none !important;
    }
    .nav-actions {
      display: none !important;
    }
    .nav-hamburger {
      display: flex !important;
      margin-left: auto !important;
      margin-right: -8px !important;
    }

    /* Stack 2-column layouts on tablet */
    .hero {
      grid-template-columns: 1fr;
      max-height: none;
      min-height: 0;
    }
    .hero-image {
      height: 520px;
    }
    .hero-content {
      padding: 3rem 2.5rem;
    }
    .router-row {
      flex-direction: column;
      height: auto;
      gap: 14px;
    }
    .r-card-wide,
    .r-card-narrow {
      flex: none !important;
      width: 100% !important;
      min-width: 0 !important;
      display: block !important;
    }
    .r-wide-content {
      display: flex !important;
      flex-direction: column;
      padding: 28px;
      gap: 14px;
    }
    .r-wide-image {
      display: none !important;
    }
    .r-narrow-content {
      display: none !important;
    }
    .r-card.r-inactive .r-wide-content {
      display: flex !important;
    }
    .r-cta {
      margin-top: 16px;
      align-self: flex-start;
    }
    .twoways-cards {
      grid-template-columns: 1fr;
    }
    .testimonials-grid {
      grid-template-columns: 1fr;
    }
    .howitworks-grid {
      grid-template-columns: 1fr;
    }
    .l349 {
      grid-template-columns: 1fr;
    }
    .l349-right {
      display: none;
    }
    .l349-section {
      min-height: auto;
      padding: 72px var(--pad-x);
    }
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: 2.5rem;
    }
  }
  @media (max-width: 768px) {
    :root {
      --pad-x: 20px;
      --pad-s: 56px;
    }
    body {
      padding-bottom: 78px;
    }
    .nav {
      height: 64px !important;
    }
    .nav-logo {
      height: 48px !important;
    }
    .nav-logo img {
      height: 44px !important;
      width: auto !important;
      max-width: none !important;
    }
    .nav-actions {
      display: none !important;
    }
    .nav-hamburger {
      display: flex !important;
    }
    .mobile-cta-bar {
      display: flex !important;
    }
    .hero {
      grid-template-columns: 1fr;
      max-height: none;
      min-height: 0;
    }
    .hero-image {
      height: 280px;
    }
    .hero-content {
      padding: 2.5rem 1.75rem;
    }
    .nav-links {
      display: none;
    }
    .router-row {
      flex-direction: column;
      height: auto;
      gap: 12px;
    }
    .r-card-wide,
    .r-card-narrow {
      flex: none !important;
      width: 100% !important;
      min-width: 0 !important;
    }
    .r-card-wide,
    .r-card-narrow {
      display: block !important;
    }
    .r-wide-content {
      display: flex !important;
      flex-direction: column;
      padding: 20px;
    }
    .r-wide-image {
      display: none !important;
    }
    .r-narrow-content {
      display: none !important;
    }
    .r-card.r-inactive .r-wide-content {
      display: flex !important;
    }
    .r-card.r-inactive .r-narrow-content {
      display: none !important;
    }
    .r-cta {
      margin-top: 12px;
    }
    .twoways-cards,
    .testimonials-grid {
      grid-template-columns: 1fr;
    }
    .howitworks-grid {
      grid-template-columns: 1fr;
    }
    .l349 {
      grid-template-columns: 1fr;
    }
    .l349-right {
      display: none;
    }
    .l349-section {
      min-height: auto;
      padding: 56px var(--pad-x);
    }
    .footer-grid {
      grid-template-columns: 1fr;
    }
    .footer-bottom {
      flex-direction: column;
      gap: 1rem;
    }
    .provider-actions {
      flex-direction: column;
      align-items: center;
    }
    .hero-actions .btn {
      width: 100%;
      justify-content: center;
    }
  }

  /* Mobile sticky CTA bar */
  .mobile-cta-bar {
    display: none;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 99;
    background: #fff;
    border-top: 1px solid var(--border);
    box-shadow: 0 -4px 20px rgba(10, 45, 82, 0.08);
    padding: 10px 12px calc(10px + env(safe-area-inset-bottom, 0px));
    gap: 8px;
    align-items: stretch;
  }
  .mobile-cta-bar .btn {
    flex: 1;
    justify-content: center;
    padding: 14px 12px;
    font-size: 14px;
    margin: 0;
  }
</style>

<BaseLayout
  seo={page?.seo ?? null}
  title={page?.seo?.metaTitle ??
    'Better You Therapy | Licensed Mental Health Care in Southeast Florida'}
>
  <!-- Mobile menu drawer -->
  <div
    class="mobile-menu"
    id="mobileMenu"
    onclick="if(event.target===this){this.classList.remove('open');document.body.style.overflow=''}"
  >
    <div class="mobile-menu-panel" role="dialog" aria-label="Main menu">
      <div class="mobile-menu-head">
        <span
          style="font-family:'Manrope',sans-serif;font-weight:800;color:var(--navy-deep);font-size:17px;letter-spacing:-.01em"
          >Better You Therapy</span
        >
        <button
          class="mobile-menu-close"
          aria-label="Close menu"
          onclick="document.getElementById('mobileMenu').classList.remove('open');document.body.style.overflow=''"
        >
          <svg
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.2"
            stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"></path></svg
          >
        </button>
      </div>
      <a href="/communities/" class="mm-link">For Facilities</a>
      <a href="/patients/" class="mm-link">For Patients</a>
      <a href="/providers/" class="mm-link">For Therapists</a>
      <a href="/about/" class="mm-link">About</a>
      <div class="mobile-menu-actions">
        <a href="/individual-therapy/" class="btn btn-coral">Book a Session</a>
        <a href="/referral/" class="btn btn-outline-ink">Refer a Resident</a>
      </div>
    </div>
  </div>

  <!-- ── BLOCK 1: HERO ── -->
  <section class="hero">
    <div class="hero-image">
      <img
        src={page?.heroImage?.asset?.url ??
          'https://images.unsplash.com/photo-1544027993-37dbfe43562a?w=1400&q=85&auto=format&fit=crop&crop=faces'}
        alt={page?.heroImage?.alt ??
          'Licensed therapist in warm conversation with a senior patient'}
        onerror="this.style.display='none'"
      />
    </div>
    <div class="hero-content">
      <div class="hero-eyebrow">
        {page?.heroEyebrow ?? 'Licensed Mental Health Care · Southeast Florida & Statewide'}
      </div>
      <h1>
        {
          page?.heroHeadline ??
            'Mental health care should reach people where they are — not just the ones whose families can navigate the system.'
        }
      </h1>
      <p class="hero-subhead">
        {
          page?.heroSubhead ??
            'We bring licensed therapists on-site to assisted living and skilled nursing facilities across Southeast Florida, and connect adults and families with therapists online — anywhere in Florida. We handle everything.'
        }
      </p>
      <div class="hero-actions">
        <a href={page?.heroPrimaryCta?.href ?? '/patients/'} class="btn btn-coral"
          >{page?.heroPrimaryCta?.label ?? 'Book a Session'} &rarr;</a
        >
        <a href={page?.heroSecondaryCta?.href ?? '/referral/'} class="btn btn-outline-ink"
          >{page?.heroSecondaryCta?.label ?? 'Refer a Resident'} &rarr;</a
        >
      </div>
    </div>
  </section>

  <!-- ── BLOCK 2: AUDIENCE ROUTER ── -->
  <section class="router-section">
    <div class="router-header fade-up">
      <p class="eyebrow">{page?.routerEyebrow ?? 'Your path starts here'}</p>
      <h2>{page?.routerHeading ?? 'Who are you here to help?'}</h2>
      <p>{page?.routerSubhead ?? 'Everyone deserves care that meets them where they are.'}</p>
    </div>
    <div class="router-row" id="routerRow">
      {
        routerCards.length > 0 ? (
          routerCards.map((card, idx) => (
            <div
              class={`r-card ${idx === 0 ? 'r-card-wide r-active' : 'r-card-narrow r-inactive'}`}
              data-idx={idx}
              onclick={`activateRouterCard(${idx})`}
            >
              <div class="r-wide-content" style={idx !== 0 ? 'display:none' : ''}>
                <div>
                  <p class="r-tagline">{card.tagline}</p>
                  <h3 class="r-heading">{card.heading}</h3>
                  <p class="r-body">{card.bodyExpanded ?? card.bodyCollapsed}</p>
                </div>
                {card.cta && (
                  <a href={card.cta.href} class="r-cta" onclick="event.stopPropagation()">
                    {card.cta.label}
                    <svg
                      width="14"
                      height="14"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                    >
                      <path d="M5 12h14M12 5l7 7-7 7" />
                    </svg>
                  </a>
                )}
              </div>
              {card.image?.asset?.url && (
                <div
                  class="r-wide-image"
                  style={`${idx !== 0 ? 'display:none;' : ''}background-image:url('${card.image.asset.url}');background-size:cover;background-position:center;`}
                />
              )}
              <div class="r-narrow-content" style={idx === 0 ? 'display:none' : ''}>
                <div class="r-icon">
                  <svg
                    width="22"
                    height="22"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="#9CAF88"
                    stroke-width="2"
                    stroke-linecap="round"
                  >
                    <>
                      <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                      <circle cx="12" cy="7" r="4" />
                    </>
                  </svg>
                </div>
                <div>
                  <p class="r-tagline">{card.tagline}</p>
                  <h3 class="r-heading">{card.heading}</h3>
                  <p class="r-body">{card.bodyCollapsed}</p>
                </div>
                {card.cta && (
                  <a href={card.cta.href} class="r-cta" onclick="event.stopPropagation()">
                    {card.cta.label}
                    <svg
                      width="14"
                      height="14"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                    >
                      <path d="M5 12h14M12 5l7 7-7 7" />
                    </svg>
                  </a>
                )}
              </div>
            </div>
          ))
        ) : (
          <>
            <div class="r-card r-card-wide r-active" data-idx="0" onclick="activateRouterCard(0)">
              <div class="r-wide-content">
                <div>
                  <p class="r-tagline">Facilities</p>
                  <h3 class="r-heading">For Wellness Directors &amp; Facility Admins</h3>
                  <p class="r-body">
                    Your residents deserve mental health care. We make sure nothing stands in the
                    way. Zero cost. We handle billing, scheduling, and documentation.
                  </p>
                </div>
                <a href="/communities/" class="r-cta" onclick="event.stopPropagation()">
                  Refer a Resident
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
              <div
                class="r-wide-image"
                style="background-image:url('https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?w=700&q=85&auto=format&fit=crop');background-size:cover;background-position:center;"
              />
              <div class="r-narrow-content" style="display:none">
                <div class="r-icon">
                  <svg
                    width="22"
                    height="22"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="#E17B5D"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  >
                    <>
                      <path d="M3 21h18" />
                      <path d="M5 21V7l7-4 7 4v14" />
                      <path d="M9 9h1m4 0h1M9 13h1m4 0h1M9 17h1m4 0h1" />
                    </>
                  </svg>
                </div>
                <div>
                  <p class="r-tagline">Facilities</p>
                  <h3 class="r-heading">For Wellness Directors &amp; Facility Admins</h3>
                  <p class="r-body">
                    Zero cost to your facility. We handle billing, scheduling, and documentation.
                  </p>
                </div>
                <a href="/communities/" class="r-cta" onclick="event.stopPropagation()">
                  Refer a Resident
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>

            <div
              class="r-card r-card-narrow r-inactive"
              data-idx="1"
              onclick="activateRouterCard(1)"
            >
              <div class="r-wide-content" style="display:none">
                <div>
                  <p class="r-tagline">Patients &amp; Families</p>
                  <h3 class="r-heading">For Adults, Families &amp; Caregivers</h3>
                  <p class="r-body">
                    You've decided it's time to get help — or someone you love has. Let's make that
                    happen this week. No waitlist. No runaround.
                  </p>
                </div>
                <a href="/patients/" class="r-cta" onclick="event.stopPropagation()">
                  Book a Session
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
              <div
                class="r-wide-image"
                style="display:none;background-image:url('https://images.unsplash.com/photo-1527137342181-19aab11a8ee8?w=700&q=85&auto=format&fit=crop');background-size:cover;background-position:center;"
              />
              <div class="r-narrow-content">
                <div class="r-icon">
                  <svg
                    width="22"
                    height="22"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="#9CAF88"
                    stroke-width="2"
                    stroke-linecap="round"
                  >
                    <>
                      <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                      <circle cx="12" cy="7" r="4" />
                    </>
                  </svg>
                </div>
                <div>
                  <p class="r-tagline">Patients &amp; Families</p>
                  <h3 class="r-heading">For Adults, Families &amp; Caregivers</h3>
                  <p class="r-body">
                    You've decided it's time to get help — or someone you love has. Let's make that
                    happen this week.
                  </p>
                </div>
                <a href="/patients/" class="r-cta" onclick="event.stopPropagation()">
                  Book a Session
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>

            <div
              class="r-card r-card-narrow r-inactive"
              data-idx="2"
              onclick="activateRouterCard(2)"
            >
              <div class="r-wide-content" style="display:none">
                <div>
                  <p class="r-tagline">Therapists</p>
                  <h3 class="r-heading">For Licensed Therapists in Florida</h3>
                  <p class="r-body">
                    You became a therapist to do therapy. We built the model that makes that
                    possible. Transparent revenue split from day one.
                  </p>
                </div>
                <a href="/providers/" class="r-cta" onclick="event.stopPropagation()">
                  Work with Us
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
              <div
                class="r-wide-image"
                style="display:none;background-image:url('https://images.unsplash.com/photo-1559839734-2b71ea197ec2?w=700&q=85&auto=format&fit=crop');background-size:cover;background-position:center;"
              />
              <div class="r-narrow-content">
                <div class="r-icon">
                  <svg
                    width="22"
                    height="22"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="#9CAF88"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  >
                    <>
                      <rect x="2" y="7" width="20" height="14" rx="2" />
                      <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16" />
                    </>
                  </svg>
                </div>
                <div>
                  <p class="r-tagline">Therapists</p>
                  <h3 class="r-heading">For Licensed Therapists in Florida</h3>
                  <p class="r-body">
                    You became a therapist to do therapy. We built the model that makes that
                    possible. Transparent revenue split from day one.
                  </p>
                </div>
                <a href="/providers/" class="r-cta" onclick="event.stopPropagation()">
                  Work with Us
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
          </>
        )
      }
    </div>
  </section>

  <!-- ── BLOCK 3: BELIEF BAND ── -->
  <section class="belief">
    <div class="belief-inner fade-up">
      <div class="belief-rule"></div>
      <h2>"{page?.beliefQuote ?? 'Mental health care belongs to everyone.'}"</h2>
      <p>
        {
          page?.beliefBody ??
            "Mental health care shouldn't depend on where you live, how much you earn, or how well you can navigate the system. We built Better You Therapy to close that gap."
        }
      </p>
    </div>
  </section>

  <!-- ── BLOCK 4: TWO WAYS ── -->
  <section class="twoways">
    <div style="max-width:var(--max-w);margin:0 auto;">
      <div class="twoways-header fade-up">
        <p class="eyebrow" style="margin-bottom:.75rem">
          {page?.twoWaysEyebrow ?? 'Two Ways We Work'}
        </p>
        <h2>
          {page?.twoWaysHeading ?? 'Two Ways to Get Mental Health Care Through Better You Therapy'}
        </h2>
        <p>
          {
            page?.twoWaysSubhead ??
              'Whether you need a therapist to come to you, or want to connect from wherever you are — we have a path for you.'
          }
        </p>
      </div>
      <div class="twoways-cards fade-up">
        {
          twoWaysTracks.length > 0 ? (
            twoWaysTracks.map((track, idx) => (
              <div class={`twoways-card${idx === 0 ? ' twoways-card-lead' : ''}`}>
                <div
                  class="twoways-card-bg"
                  style={`background-image:url('${track.image?.asset?.url ?? (idx === 0 ? 'https://images.unsplash.com/photo-1611532736597-de2d4265fba3?w=900&q=85&auto=format&fit=crop' : 'https://images.unsplash.com/photo-1576091160550-2173dba999ef?w=900&q=85&auto=format&fit=crop')}');background-color:#0A2D52;`}
                />
                <div class="twoways-card-overlay" />
                <div class="twoways-card-content">
                  <div class="twoways-card-label">{track.label}</div>
                  <h3>{track.heading}</h3>
                  {track.body && <p>{track.body}</p>}
                  {track.cta && (
                    <a
                      href={track.cta.href}
                      class={`btn ${idx === 0 ? 'btn-white' : 'btn-white-outline'}`}
                    >
                      {track.cta.label} &rarr;
                    </a>
                  )}
                </div>
              </div>
            ))
          ) : (
            <>
              <div class="twoways-card twoways-card-lead">
                <div
                  class="twoways-card-bg"
                  style="background-image:url('https://images.unsplash.com/photo-1611532736597-de2d4265fba3?w=900&q=85&auto=format&fit=crop');background-color:#0A2D52;"
                />
                <div class="twoways-card-overlay" />
                <div class="twoways-card-content">
                  <div class="twoways-card-label">Teletherapy — Anywhere in Florida</div>
                  <h3>Therapy available this week, from wherever you are</h3>
                  <p>
                    Licensed therapists available via secure video. Adults, couples, and families.
                    Cash-pay or insurance. No waitlist.
                  </p>
                  <a href="/individual-therapy/" class="btn btn-white">
                    Book a Session &rarr;
                  </a>
                </div>
              </div>
              <div class="twoways-card">
                <div
                  class="twoways-card-bg"
                  style="background-image:url('https://images.unsplash.com/photo-1576091160550-2173dba999ef?w=900&q=85&auto=format&fit=crop');background-color:#0A2D52;"
                />
                <div class="twoways-card-overlay" />
                <div class="twoways-card-content">
                  <div class="twoways-card-label">In-Facility — Southeast Florida</div>
                  <h3>On-site mental health care at your facility</h3>
                  <p>
                    Licensed therapists placed inside assisted living, skilled nursing, and
                    continuing care facilities. Medicare-billed. Zero cost to the facility or
                    resident.
                  </p>
                  <a href="/communities/" class="btn btn-white-outline">
                    Refer a Resident &rarr;
                  </a>
                </div>
              </div>
            </>
          )
        }
      </div>
    </div>
  </section>

  <!-- ── BLOCK 5: CONDITIONS ── -->
  <div class="l349-section-header fade-up">
    <p class="eyebrow" style="margin-bottom:.75rem">{page?.conditionsEyebrow ?? 'What We Treat'}</p>
    <h2>{page?.conditionsHeading ?? 'Conditions We Treat'}</h2>
    <p>
      {
        page?.conditionsSubhead ??
          'Our licensed therapists work with adults, seniors, couples, and families across a wide range of mental health conditions.'
      }
    </p>
  </div>
  <section class="l349" id="l349">
    <div class="l349-left">
      {
        conditionsList.length > 0 ? (
          conditionsList.map((condition, idx) => (
            <div class="l349-section" data-section={idx}>
              <p class="l349-tagline">{condition.tagline}</p>
              <h2 class="l349-h2">{condition.heading}</h2>
              {condition.body && <p class="l349-body">{condition.body}</p>}
              <div class="l349-actions">
                <a href="/individual-therapy/" class="l349-btn-outline">
                  Book a session
                </a>
                <a href="/referral/" class="l349-btn-ghost">
                  Refer a resident{' '}
                  <svg
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
          ))
        ) : (
          <>
            <div class="l349-section" data-section="0">
              <p class="l349-tagline">Depression</p>
              <h2 class="l349-h2">Depression &amp; Anxiety</h2>
              <p class="l349-body">
                Persistent sadness, worry, and overwhelm are treatable. Our licensed therapists
                provide evidence-based care that meets you or your loved one where they are.
              </p>
              <div class="l349-actions">
                <a href="/individual-therapy/" class="l349-btn-outline">
                  Book a session
                </a>
                <a href="/referral/" class="l349-btn-ghost">
                  Refer a resident{' '}
                  <svg
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
            <div class="l349-section" data-section="1">
              <p class="l349-tagline">Grief &amp; Loss</p>
              <h2 class="l349-h2">Grief, Loss &amp; Life Transitions</h2>
              <p class="l349-body">
                Loss of a loved one, a role, or a way of life — grief takes many forms. We provide a
                safe space to process and move forward.
              </p>
              <div class="l349-actions">
                <a href="/individual-therapy/" class="l349-btn-outline">
                  Book a session
                </a>
                <a href="/referral/" class="l349-btn-ghost">
                  Refer a resident{' '}
                  <svg
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
            <div class="l349-section" data-section="2">
              <p class="l349-tagline">Trauma</p>
              <h2 class="l349-h2">PTSD &amp; Trauma</h2>
              <p class="l349-body">
                Trauma-informed care from licensed therapists who understand how past experiences
                shape present wellbeing.
              </p>
              <div class="l349-actions">
                <a href="/individual-therapy/" class="l349-btn-outline">
                  Book a session
                </a>
                <a href="/referral/" class="l349-btn-ghost">
                  Refer a resident{' '}
                  <svg
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
            <div class="l349-section" data-section="3">
              <p class="l349-tagline">Relationships</p>
              <h2 class="l349-h2">Relationships, Couples &amp; Family</h2>
              <p class="l349-body">
                Communication, conflict, and connection — for couples, families, and caregivers
                navigating change together.
              </p>
              <div class="l349-actions">
                <a href="/individual-therapy/" class="l349-btn-outline">
                  Book a session
                </a>
                <a href="/referral/" class="l349-btn-ghost">
                  Refer a resident{' '}
                  <svg
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                  >
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
          </>
        )
      }
    </div>
    <div class="l349-right">
      <div class="l349-img-wrap" id="l349Images">
        {
          conditionsList.length > 0 ? (
            conditionsList.map((condition, idx) => (
              <div
                class={`l349-img${idx === 0 ? ' active' : ''}`}
                data-img={idx}
                style={`background:url('${condition.image?.asset?.url ?? 'https://images.unsplash.com/photo-1493836512294-502baa1986e2?w=700&q=85&auto=format&fit=crop'}') center/cover;`}
              />
            ))
          ) : (
            <>
              <div
                class="l349-img active"
                data-img="0"
                style="background:url('https://images.unsplash.com/photo-1493836512294-502baa1986e2?w=700&q=85&auto=format&fit=crop') center/cover;"
              />
              <div
                class="l349-img"
                data-img="1"
                style="background:url('https://images.unsplash.com/photo-1499209974431-9dddcece7f88?w=700&q=85&auto=format&fit=crop') center/cover;"
              />
              <div
                class="l349-img"
                data-img="2"
                style="background:url('https://images.unsplash.com/photo-1516302752625-fcc3c50ae61f?w=700&q=85&auto=format&fit=crop') center/cover;"
              />
              <div
                class="l349-img"
                data-img="3"
                style="background:url('https://images.unsplash.com/photo-1529156069898-49953e39b3ac?w=700&q=85&auto=format&fit=crop') center/cover;"
              />
            </>
          )
        }
      </div>
    </div>
  </section>

  <!-- ── BLOCK 6: HOW IT WORKS ── -->
  <section class="howitworks">
    <div style="max-width:var(--max-w);margin:0 auto;">
      <div class="howitworks-header fade-up">
        <p class="eyebrow" style="margin-bottom:.75rem">
          {page?.howItWorksEyebrow ?? 'The Process'}
        </p>
        <h2>{page?.howItWorksHeading ?? 'Getting Started Is Simple — Either Way'}</h2>
      </div>
      <div class="howitworks-grid fade-up">
        <div class="howitworks-track howitworks-track-tele">
          <div class="howitworks-track-label">
            {page?.teletherapyTrackLabel ?? 'Online Therapy Track'}
          </div>
          <div class="steps-wrap">
            {
              teletherapySteps.length > 0 ? (
                teletherapySteps.map((step) => (
                  <div class="step">
                    <div class="step-num tele-num">{step.stepNumber}</div>
                    <div class="step-text">
                      <h4>{step.heading}</h4>
                      {step.body && <p>{step.body}</p>}
                    </div>
                  </div>
                ))
              ) : (
                <>
                  <div class="step">
                    <>
                      <div class="step-num tele-num">1</div>
                      <div class="step-text">
                        <>
                          <h4>Tell Us What You're Looking For</h4>
                          <p>
                            A short intake — your needs, availability, and preferences. Under 5
                            minutes.
                          </p>
                        </>
                      </div>
                    </>
                  </div>
                  <div class="step">
                    <>
                      <div class="step-num tele-num">2</div>
                      <div class="step-text">
                        <>
                          <h4>We Match You With a Licensed Therapist</h4>
                          <p>
                            We handle the matching. You get a therapist suited to your specific
                            needs.
                          </p>
                        </>
                      </div>
                    </>
                  </div>
                  <div class="step">
                    <>
                      <div class="step-num tele-num">3</div>
                      <div class="step-text">
                        <>
                          <h4>Your First Session Is Available This Week</h4>
                          <p>No waitlist. Secure video sessions on your schedule.</p>
                        </>
                      </div>
                    </>
                  </div>
                </>
              )
            }
          </div>
          <div class="track-cta">
            <a href={page?.teletherapyCta?.href ?? '/individual-therapy/'} class="btn btn-sage"
              >{page?.teletherapyCta?.label ?? 'Book a Session'} &rarr;</a>
          </div>
        </div>
        <div class="howitworks-track howitworks-track-facility">
          <div class="howitworks-track-label">
            {page?.facilityTrackLabel ?? 'In-Facility Track'}
          </div>
          <div class="steps-wrap">
            {
              facilitySteps.length > 0 ? (
                facilitySteps.map((step) => (
                  <div class="step">
                    <div class="step-num facility-num">{step.stepNumber}</div>
                    <div class="step-text">
                      <h4>{step.heading}</h4>
                      {step.body && <p>{step.body}</p>}
                    </div>
                  </div>
                ))
              ) : (
                <>
                  <div class="step">
                    <>
                      <div class="step-num facility-num">1</div>
                      <div class="step-text">
                        <>
                          <h4>You Refer a Resident</h4>
                          <p>
                            A simple referral form. We take it from there — no paperwork burden for
                            your team.
                          </p>
                        </>
                      </div>
                    </>
                  </div>
                  <div class="step">
                    <>
                      <div class="step-num facility-num">2</div>
                      <div class="step-text">
                        <>
                          <h4>We Handle Scheduling and Medicare Billing</h4>
                          <p>
                            Consent, credentialing, billing to Medicare Part B. All handled by BYT.
                          </p>
                        </>
                      </div>
                    </>
                  </div>
                  <div class="step">
                    <>
                      <div class="step-num facility-num">3</div>
                      <div class="step-text">
                        <>
                          <h4>Your Therapist Arrives On-Site, Weekly</h4>
                          <p>
                            Your residents receive care in their own room. Weekly visits. Zero cost
                            to the facility.
                          </p>
                        </>
                      </div>
                    </>
                  </div>
                </>
              )
            }
          </div>
          <div class="track-cta">
            <a href={page?.facilityCta?.href ?? '/referral/'} class="btn btn-coral"
              >{page?.facilityCta?.label ?? 'Refer a Resident'} &rarr;</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── BLOCK 7: TESTIMONIALS ── -->
  <section class="testimonials">
    <div style="max-width:var(--max-w);margin:0 auto;">
      <div class="testimonials-header fade-up">
        <p class="eyebrow" style="margin-bottom:.75rem">
          {page?.testimonialsEyebrow ?? 'Real Voices'}
        </p>
        <h2>{page?.testimonialsHeading ?? 'What Our Clients and Partners Are Saying'}</h2>
        <p>
          {
            page?.testimonialsSubhead ??
              'From the families and facilities we serve — in their own words.'
          }
        </p>
      </div>
      <div class="testimonials-grid fade-up">
        {
          testimonialsList.length > 0 ? (
            testimonialsList.map((t) => (
              <div class="testimonial-card">
                <div class="testimonial-quote-mark">"</div>
                <p class="testimonial-text">{t.quote}</p>
                <div class="testimonial-attribution">
                  <div class="testimonial-avatar">
                    {t.authorInitials ?? t.authorName.slice(0, 2).toUpperCase()}
                  </div>
                  <div>
                    <div class="testimonial-name">{t.authorName}</div>
                    <div class="testimonial-role">
                      {[t.authorRole, t.authorOrg].filter(Boolean).join(' · ')}
                    </div>
                  </div>
                </div>
              </div>
            ))
          ) : (
            <>
              <div class="testimonial-card">
                <div class="testimonial-quote-mark">"</div>
                <p class="testimonial-text">
                  Better You placed a licensed clinician on-site within two weeks. No billing burden
                  on our team, and our residents have weekly continuity of care they didn't have
                  before. [Sample testimonial]
                </p>
                <div class="testimonial-attribution">
                  <div
                    class="testimonial-avatar"
                    style="background:url('https://images.unsplash.com/photo-1573497019940-1c28c88b4f3e?w=160&q=90&auto=format&fit=crop') center/cover;color:transparent;"
                  >
                    WD
                  </div>
                  <div>
                    <div class="testimonial-name">Director of Wellness</div>
                    <div class="testimonial-role">Assisted Living · Palm Beach County</div>
                  </div>
                </div>
              </div>
              <div class="testimonial-card">
                <div class="testimonial-quote-mark">"</div>
                <p class="testimonial-text">
                  My mom had been on a waitlist for months. One call to Better You and she had her
                  first session that same week — right from her apartment. [Sample testimonial]
                </p>
                <div class="testimonial-attribution">
                  <div
                    class="testimonial-avatar"
                    style="background:url('https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=160&q=90&auto=format&fit=crop') center/cover;color:transparent;"
                  >
                    FM
                  </div>
                  <div>
                    <div class="testimonial-name">Adult Daughter</div>
                    <div class="testimonial-role">Family member · Southeast Florida</div>
                  </div>
                </div>
              </div>
            </>
          )
        }
      </div>
    </div>
  </section>

  <!-- ── BLOCK 8: PROVIDER TEASER ── -->
  <section class="provider">
    <div
      class="provider-bg"
      style={`background-image:url('${page?.providerTeaserImage?.asset?.url ?? 'https://images.unsplash.com/photo-1582750433449-648ed127bb54?w=1600&q=80&auto=format&fit=crop'}');`}
    >
    </div>
    <div class="provider-overlay"></div>
    <div class="provider-inner fade-up">
      <p class="eyebrow" style="margin-bottom:1rem;color:rgba(255,255,255,.75)">
        {page?.providerTeaserEyebrow ?? 'Join the Team'}
      </p>
      <h2>{page?.providerTeaserHeading ?? 'Are You a Licensed Therapist in Florida?'}</h2>
      <p>
        {
          page?.providerTeaserBody ??
            'Facility-based positions in Southeast Florida and teletherapy positions statewide. Transparent revenue split from day one. We source the patients — you deliver the clinical care.'
        }
      </p>
      <div class="provider-tags">
        <span class="provider-tag">Psychologists</span><span class="provider-tag">LCSWs</span><span
          class="provider-tag">LMHCs</span
        ><span class="provider-tag">LPCs</span><span class="provider-tag">LMFTs</span><span
          class="provider-tag">Facility-based</span
        ><span class="provider-tag">Teletherapy</span>
      </div>
      <div class="provider-actions">
        <a href={page?.providerTeaserPrimaryCta?.href ?? '/providers/'} class="btn btn-coral"
          >{page?.providerTeaserPrimaryCta?.label ?? 'See Open Positions'} &rarr;</a
        >
        <a
          href={page?.providerTeaserSecondaryCta?.href ?? '/providers/'}
          class="btn btn-outline-white">{page?.providerTeaserSecondaryCta?.label ?? 'Learn More'}</a
        >
      </div>
    </div>
  </section>
</BaseLayout>

<script is:inline>
  // ── BLOCK 2: Router accordion
  function activateRouterCard(idx) {
    const isMobile = window.innerWidth <= 768;
    document.querySelectorAll('.r-card').forEach((c, i) => {
      c.classList.remove('r-active', 'r-inactive');
      c.classList.add(i === idx ? 'r-active' : 'r-inactive');
      const wc = c.querySelector('.r-wide-content');
      const wi = c.querySelector('.r-wide-image');
      const nc = c.querySelector('.r-narrow-content');
      if (isMobile) {
        // On mobile: all cards show wide-content stacked
        if (wc) wc.style.display = 'flex';
        if (wi) wi.style.display = 'none';
        if (nc) nc.style.display = 'none';
        c.style.display = 'block';
        c.style.flexDirection = '';
      } else if (i === idx) {
        if (wc) wc.style.display = 'flex';
        if (wi) wi.style.display = 'block';
        if (nc) nc.style.display = 'none';
        c.style.display = 'flex';
        c.style.flexDirection = 'row';
      } else {
        if (wc) wc.style.display = 'none';
        if (wi) wi.style.display = 'none';
        if (nc) nc.style.display = 'flex';
        c.style.display = '';
        c.style.flexDirection = '';
      }
    });
  }
  document.querySelectorAll('.r-card').forEach((c, i) => {
    c.classList.add(i === 0 ? 'r-active' : 'r-inactive');
  });
  activateRouterCard(0);
  window.addEventListener('resize', () =>
    activateRouterCard(
      [...document.querySelectorAll('.r-card')].findIndex((c) =>
        c.classList.contains('r-active'),
      ) || 0,
    ),
  );

  // ── BLOCK 5: sticky scroll image cross-fade
  const l349Imgs = document.querySelectorAll('[data-img]');
  function updateConditionImage() {
    const vh = window.innerHeight;
    const l349 = document.getElementById('l349');
    if (!l349) return;
    const rect = l349.getBoundingClientRect();
    const scrolledInto = -rect.top;
    if (scrolledInto < 0) return;
    const active = Math.min(Math.floor(scrolledInto / vh), l349Imgs.length - 1);
    l349Imgs.forEach((img, i) => {
      img.style.opacity = i === active ? '1' : '0';
      img.classList.toggle('active', i === active);
    });
  }
  l349Imgs.forEach((img, i) => {
    img.style.transition = 'opacity 0.35s ease';
    if (i > 0) img.style.opacity = '0';
  });
  window.addEventListener('scroll', updateConditionImage, { passive: true });
  updateConditionImage();

  // ── Scroll fade-up
  const fadeObs = new IntersectionObserver(
    (entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          fadeObs.unobserve(e.target);
        }
      });
    },
    { threshold: 0.12, rootMargin: '0px 0px -40px 0px' },
  );
  document.querySelectorAll('.fade-up').forEach((el) => fadeObs.observe(el));
</script>
